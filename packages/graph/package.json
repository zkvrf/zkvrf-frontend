{
  "name": "@zkvrf/graph",
  "license": "UNLICENSED",
  "version": "0.0.0",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ zkvrf-test",
    "create-local": "graph create --node http://localhost:8020/ zkvrf-test",
    "remove-local": "graph remove --node http://localhost:8020/ zkvrf-test",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 zkvrf-test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.60.0",
    "@graphprotocol/graph-ts": "0.31.0"
  },
  "devDependencies": {
    "matchstick-as": "0.6.0"
  }
}
